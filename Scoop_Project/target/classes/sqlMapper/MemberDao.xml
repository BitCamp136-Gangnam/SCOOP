<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.scoop.dao.MemberDao">
	<!--
	     name,dname, drank,address,idtime,mrank,loginnum
	

	 -->
	 <!-- 일반회원가입 -->
	 <insert id="insertMember" parameterType="kr.or.scoop.vo.Member">
	 	insert into member(email, pwd, name,idtime,loginnum) values (#{email},#{pwd},#{name},sysdate,1)
	 </insert>

	<!-- return multi row >> User 객체 여러개 >> List<User> (x) -->
	<select id="getUsers" resultType="kr.or.bit.dto.User">
		select empno , ename from emp
	</select>
</mapper>